# VisCurate — pinned environment (replicates the current dev box: Python 3.13, Windows).
#
# This is the exact, working environment for Phases 0–1 (the dependency-light harness).
# Versions match what is actually installed and tested. The authoritative dependency
# *specification* lives in pyproject.toml; this file pins it for reproducible installs.
#
# Recommended setup (mirrors how this env was built):
#     python -m pip install -r requirements.txt
#     python -m pip install -e . --no-deps      # register the `viscurate` package
#
# (`-e . --no-deps` avoids re-resolving opencv: this box provides cv2 via
#  opencv-contrib-python, while pyproject declares the canonical opencv-python.)

# ---- Core runtime (Phases 0–1) -------------------------------------------------------
numpy==2.2.6
Pillow==12.2.0
opencv-contrib-python==4.12.0.88   # provides `cv2`; a superset of opencv-python
pydantic==2.12.5
pydantic-core==2.41.5              # pinned transitive of pydantic
PyYAML==6.0.2
structlog==26.1.0

# ---- Dev tooling ---------------------------------------------------------------------
pytest==9.1.1
ruff==0.15.18
mypy==1.17.1
mypy-extensions==1.1.0
types-PyYAML==6.0.12.20260518

# ---- Shared transitive pins (for byte-for-byte reproducibility) ----------------------
annotated-types==0.7.0
typing_extensions==4.15.0
iniconfig==2.3.0
pluggy==1.6.0
packaging==24.2

# ---- ML comparators (Phase 3, the `[ml]` extra) --------------------------------------
# torch is already present on this box (CPU build); the rest are NOT yet installed and
# arrive with Phase 3. Uncomment when building the output-grounded comparators.
torch==2.10.0
# lpips>=0.1.4
# timm>=1.0
# open-clip-torch>=2.24
# scikit-image>=0.22
