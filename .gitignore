# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Local secrets
.env

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# VisCurate data/results — large, generated, reproducible from manifests.
# Probe images and oracle outputs are regenerated from seeds; never committed raw.
data/probe_images/
data/oracle/
# Corrupted libraries L_ρ — regenerated deterministically from (L0 + corruption log).
data/corruption/
results/
# Local pre-Phase-4 results snapshot — large, generated, not for the repo.
results_before_phase4/
*.npz
*.npy

# Manifests and configs ARE tracked (small, define reproducibility) — keep them.
!data/**/manifest.json

# Server logs (start_vlm.sh / run.sh)
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local tool settings
.claude/settings.local.json

# WACV-R2 fork-pair corpus (pilgram / pilgram2 / imgaug) vendored for scripts/wacv_r2 — third-party
# code, 8.7 MB. Recreate with:
#   pip install --target .corpusb_libs pilgram==2.0.0 pilgram2==2.0.10 imgaug==0.4.0
.corpusb_libs/
